# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed reports/*.jsonl directly with only the stdlib, so it needs streamlit only.
# The CLI/package under commit_provenance/ has its own deps in pyproject.toml.
streamlit>=1.40
