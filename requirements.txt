# Streamlit Community Cloud reads this file. The committed-report view needs only
# streamlit + stdlib; the interactive section imports the real readiness engine from
# the commit_provenance package, so we install this repo itself (the `.` line) to make
# `commit_provenance.scoring` importable on Streamlit Cloud.
streamlit>=1.40
.
